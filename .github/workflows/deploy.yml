name: Deploy to Hostinger

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Astro site
        run: npm run build

      - name: Deploy to Hostinger via SSH
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.195.179.239.72 }}
          username: ${{ secrets.u415774514 }}
          password: ${{ secrets.RAPforlife0420# }}
          port: ${{ secrets.65002 }}
          source: "dist/*"
          target: "/home/${{ secrets.SSH_USERNAME }}/domains/${{ secrets.DOMAIN }}/public_html"
          strip_components: 1
          overwrite: true
          rm: true
