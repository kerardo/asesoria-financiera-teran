---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description: string;
  icon: string;
  heroTitle: string;
  heroSubtitle: string;
}

const { title, description, icon, heroTitle, heroSubtitle } = Astro.props;
---

<BaseLayout title={title} description={description}>
  <!-- Hero -->
  <section class="servicio-hero">
    <div class="container">
      <span class="hero-icon">{icon}</span>
      <h1>{heroTitle}</h1>
      <p class="hero-subtitle">{heroSubtitle}</p>
      <div class="hero-cta">
        <a href="https://wa.me/5564130072?text=Hola, me interesa información sobre {title}" class="btn btn-accent" target="_blank" rel="noopener">
          Solicitar asesoría gratuita
        </a>
      </div>
    </div>
  </section>

  <!-- Content -->
  <slot />

  <!-- CTA Final -->
  <section class="cta-final">
    <div class="container">
      <div class="cta-card">
        <h2>¿Listo para dar el siguiente paso?</h2>
        <p>Agenda una asesoría gratuita y sin compromiso. Te ayudamos a encontrar la mejor solución para ti.</p>
        <div class="cta-buttons">
          <a href="https://wa.me/5564130072?text=Hola, me interesa información sobre {title}" class="btn btn-accent" target="_blank" rel="noopener">
            WhatsApp
          </a>
          <a href="/contacto" class="btn btn-primary">Enviar consulta</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Otros servicios -->
  <section class="otros-servicios">
    <div class="container">
      <h3>Explora otros servicios</h3>
      <div class="servicios-mini">
        <a href="/servicios/seguros" class="servicio-mini">🛡️ Seguros</a>
        <a href="/servicios/inversiones" class="servicio-mini">📈 Inversiones</a>
        <a href="/servicios/creditos" class="servicio-mini">💳 Créditos</a>
        <a href="/servicios/educativo" class="servicio-mini">🎓 Ahorro Educativo</a>
        <a href="/servicios/retiro" class="servicio-mini">🏖️ Planes de Retiro</a>
        <a href="/servicios/juridico" class="servicio-mini">⚖️ Asesoría Jurídica</a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Hero */
  .servicio-hero {
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-secondary) 100%);
    color: white;
    padding: 5rem 0;
    text-align: center;
  }

  .hero-icon {
    font-size: 4rem;
    display: block;
    margin-bottom: 1rem;
  }

  .servicio-hero h1 {
    font-size: 2.75rem;
    margin-bottom: 1rem;
  }

  .hero-subtitle {
    font-size: 1.2rem;
    opacity: 0.95;
    max-width: 600px;
    margin: 0 auto 2rem;
    line-height: 1.7;
  }

  .hero-cta .btn {
    font-size: 1.1rem;
    padding: 1rem 2rem;
  }

  /* CTA Final */
  .cta-final {
    padding: 4rem 0;
    background: var(--color-bg-alt);
  }

  .cta-card {
    background: var(--color-primary);
    color: white;
    padding: 3rem;
    border-radius: var(--radius);
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
  }

  .cta-card h2 {
    font-size: 1.75rem;
    margin-bottom: 1rem;
  }

  .cta-card p {
    opacity: 0.95;
    margin-bottom: 2rem;
    font-size: 1.1rem;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Otros servicios */
  .otros-servicios {
    padding: 3rem 0;
    text-align: center;
  }

  .otros-servicios h3 {
    color: var(--color-text-light);
    font-size: 1rem;
    font-weight: 500;
    margin-bottom: 1.5rem;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .servicios-mini {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    justify-content: center;
  }

  .servicio-mini {
    padding: 0.5rem 1rem;
    background: var(--color-bg-alt);
    border-radius: 20px;
    color: var(--color-text);
    font-size: 0.9rem;
    transition: all 0.3s ease;
  }

  .servicio-mini:hover {
    background: var(--color-primary);
    color: white;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .servicio-hero {
      padding: 3rem 0;
    }

    .hero-icon {
      font-size: 3rem;
    }

    .servicio-hero h1 {
      font-size: 2rem;
    }

    .hero-subtitle {
      font-size: 1rem;
    }

    .cta-card {
      padding: 2rem;
    }

    .cta-card h2 {
      font-size: 1.5rem;
    }
  }
</style>

<style is:global>
  /* Estilos globales para contenido de servicios */
  .servicio-section {
    padding: 4rem 0;
  }

  .servicio-section.alt {
    background: var(--color-bg-alt);
  }

  .servicio-content {
    max-width: 800px;
    margin: 0 auto;
  }

  .servicio-content h2 {
    color: var(--color-primary);
    font-size: 1.75rem;
    margin-bottom: 1.5rem;
  }

  .servicio-content p {
    color: var(--color-text);
    line-height: 1.8;
    margin-bottom: 1.25rem;
    font-size: 1.05rem;
  }

  .servicio-content .highlight {
    color: var(--color-primary);
    font-weight: 600;
  }

  /* Beneficios grid */
  .beneficios-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    margin: 2rem 0;
  }

  .beneficio-card {
    background: white;
    padding: 1.5rem;
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    text-align: center;
  }

  .beneficio-card .icon {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }

  .beneficio-card h3 {
    color: var(--color-primary);
    font-size: 1.1rem;
    margin-bottom: 0.5rem;
  }

  .beneficio-card p {
    color: var(--color-text-light);
    font-size: 0.95rem;
    margin: 0;
  }

  /* Lista de características */
  .caracteristicas-list {
    list-style: none;
    padding: 0;
  }

  .caracteristicas-list li {
    padding: 0.75rem 0;
    padding-left: 2rem;
    position: relative;
    border-bottom: 1px solid var(--color-border);
  }

  .caracteristicas-list li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--color-accent);
    font-weight: bold;
  }

  /* CTA intermedio */
  .cta-intermedio {
    background: linear-gradient(135deg, var(--color-accent), #d4a91e);
    color: white;
    padding: 2rem;
    border-radius: var(--radius);
    text-align: center;
    margin: 2rem 0;
  }

  .cta-intermedio h3 {
    font-size: 1.25rem;
    margin-bottom: 0.5rem;
  }

  .cta-intermedio p {
    opacity: 0.95;
    margin-bottom: 1rem;
    color: white;
  }

  .cta-intermedio .btn {
    background: white;
    color: var(--color-primary);
  }

  .cta-intermedio .btn:hover {
    background: var(--color-primary);
    color: white;
  }
</style>
